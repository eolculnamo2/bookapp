Local Setup:

git clone https://github.com/eolculnamo2/bookapp/

Open terminal and cd into the client folder in this directory. NPM run build here. If you choose to use npm run start, it must be out of this folder with second terminal described below running node server.

In second terminal, go to root directory and run node server.
